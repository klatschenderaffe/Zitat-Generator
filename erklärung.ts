const beispiel: number[] = [1, 2, 3, 4, 5];

let arraylaenge: number = beispiel.length - 1;

console.log(arraylaenge);

let zufallszahl: number = Math.round(Math.random() * arraylaenge);

console.log(zufallszahl);
